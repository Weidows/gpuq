"""gpuq - a lightweight GPU job queue / scheduler with WebUI and REST API.

Zero pip dependencies (Python stdlib only). GPU metrics come from `nvidia-smi`,
host net/disk/CPU metrics optionally from `psutil` (degraded when missing).
"""

__version__ = "0.1.2"
